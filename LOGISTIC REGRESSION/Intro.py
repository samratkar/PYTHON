from scipy.special import expit
#This is the sigmomid function
expit (2)

For the sigmoid curve (β0 = -15 and β1 = 0.065), what will be the probability of diabetes for a patient with sugar level 240?

expit (-15 + 0.065*240)
